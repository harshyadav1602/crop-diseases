This repository contains a web project that provides agricultural prediction tools and demos. It includes a frontend/ folder with a static demo and an SIH-Project/ subproject with the server and multiple services.

Quick start

Install dependencies: npm install
Run dev server: node server.js
Notes

Some model files and environment config are included for local testing. Remove large model blobs if you want a lightweight repo.
